import { axiosInstance } from '@/vendor/axiosInstance.ts';
import { {{tableNamePascalCase}} } from '@/interfaces/I{{tableNamePascalCaseSingular}}.ts';

[[LOOP(/BaseMethodsFileBased/**/**) --separator="\n\n" --template="
/**
 * {{description}}
 */
export const {{methodName}} = async {{frontendServiceMethod}} => {
  try {
{{frontendServiceContent}}
  } catch (error) {
    console.error('Error in {{methodName}} operation:', error);
    throw error;
  }
};
"]]
