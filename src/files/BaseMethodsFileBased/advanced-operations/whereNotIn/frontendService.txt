export const {{methodNameCamelCase}} = async (column, values) => {
  try {
    const response = await axiosInstance.post(`{{tableNameKebabCasePlural}}/where-not-in`, {
      column,
      values
    });
    return response.data;
  } catch (error) {
    console.error('Error in "{{methodName}}" operation:', error);
    throw error;
  }
}; 