export const {{methodNameCamelCase}} = async (column, min, max) => {
  try {
    const response = await axiosInstance.post(`{{tableNameKebabCasePlural}}/where-between`, {
      column,
      min, 
      max
    });
    return response.data;
  } catch (error) {
    console.error('Error in "{{methodName}}" operation:', error);
    throw error;
  }
}; 