[test]
# Preload happy-dom for DOM testing
preload = ["./bun-test-setup.ts"]

# Test timeout (10 seconds)
timeout = 10000

# Exclude patterns - golden test output contains generated tests that require a running server
exclude = ["**/output/**", "**/golden-projects/output/**"]

# Coverage configuration
coverageSkipTestFiles = true
coveragePathIgnorePatterns = [
  # Exclude e2e tests (run with Playwright)
  "e2e/**",

  # Exclude worktrees (separate git branches)
  ".worktrees/**",

  # Exclude build output
  ".vercel/**",

  # Exclude node_modules
  "**/node_modules/**",
]
